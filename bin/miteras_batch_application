#!/usr/bin/env ruby

require 'attendance'
require 'optparse'

opt = OptionParser.new
params = {}
opt.on('-c') {|v| params[:c] = v }
opt.on('-e') {|v| params[:e] = v }
opt.parse!(ARGV)
attendance = Attendance.new(ARGV, params)
attendance.input
